<header class="py-4">
    <nav class="flex justify-between">
        <a href="{{ url('/') }}" class="text-2xl font-bold">WEBSITE</a>
        <div>
            <a href="{{ route('login') }}" class="mx-2">Login</a>
            <a href="{{ route('register') }}" class="mx-2">Register</a>
            <a href="{{ route('contact') }}" class="mx-2">Contact</a>
        </div>
    </nav>
</header>
